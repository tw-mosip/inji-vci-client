package io.mosip.vciclient.constants

enum class NetworkContentType(val value: String) {
    APPLICATION_JSON("application/json"),
}

enum class NetworkHeader(val value: String) {
    ACCEPT("Accept"),
}